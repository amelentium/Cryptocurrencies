<UserControl x:Class="CIS.Views.CurrenciesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:CIS.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:CurrenciesViewModel}"
			 mc:Ignorable="d">
	<Grid Margin="8">
		<WrapPanel HorizontalAlignment="Right" VerticalAlignment="Top">
			<TextBox Width="128" Height="28" VerticalAlignment="Center" Padding="4"
					 Text="{Binding SearchValue}"/>
			<Button Width="28" Content="🔍"
					Command="{Binding SearchCurrencyCommand}"/>
		</WrapPanel>
		<ListBox x:Name="ItemsList" Margin="0,36,0,0"
				 ItemsSource="{Binding Currencies}"
				 d:ItemsSource="{d:SampleData ItemCount=10}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Vertical">
						<WrapPanel Margin="8 4">
							<TextBlock Text="{Binding Name}"></TextBlock>
						</WrapPanel>
						<StackPanel Orientation="Horizontal" Margin="8 4">
							<WrapPanel Margin="24 0" VerticalAlignment="Center">
								<TextBlock Text="Rank: "/>
								<TextBlock Text="{Binding Rank, StringFormat=\{0\,3\}}"/>
							</WrapPanel>
							<WrapPanel Margin="24 0" VerticalAlignment="Center">
								<TextBlock Text="Symbol: "/>
								<TextBlock Text="{Binding Symbol, StringFormat=\{0\,-7\}}"/>
							</WrapPanel>
							<WrapPanel Margin="24 0" VerticalAlignment="Center">
								<TextBlock Text="Price: "/>
								<TextBlock Text="{Binding PriceUsd, StringFormat=\{0\,10:F4\}\$}"/>
							</WrapPanel>
							<Button VerticalAlignment="Center" Padding="8 4" 
									Command="{Binding ElementName=ItemsList, Path=DataContext.CurrencyInfoNavigateCommand}"
									CommandParameter="{Binding Id}">Info</Button>
						</StackPanel>
					</StackPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</Grid>
</UserControl>
